__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Datasets are downloaded, never committed. See scripts/download_data.sh.
data/raw/
data/interim/

# Intermediate debug stages, regenerated with `lanectl detect --save-stages`
outputs/**/*_mask.png
outputs/**/*_warped.png
outputs/detect/scale_warp.png

# The full annotated video is 40 MB and is regenerated by the command in
# docs/RESULTS.md. The committed 14 s excerpt stands in for it.
outputs/video/overlay.mp4

.DS_Store
*.swp
